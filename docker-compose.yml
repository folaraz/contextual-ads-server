services:
  redis:
    image: redis:latest
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - cache:/data
    command: redis-server --save 20 1 --loglevel warning
volumes:
  cache:
    driver: local
